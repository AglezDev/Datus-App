Cambios aplicados el sábado, 4 de octubre de 2025 a las 12:25

Se ha creado `AppModule.kt` en `datus.app.com.di` para proporcionar una instancia de `android.content.Context` a través de Hilt. Esto resuelve el error de compilación `[Dagger/MissingBinding] android.content.Context cannot be provided without an @Provides-annotated method`, que surgió al introducir `DataStoreManager` que requiere un `Context`.