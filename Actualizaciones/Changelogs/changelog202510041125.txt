Cambios aplicados el sábado, 4 de octubre de 2025 a las 11:25

Se han realizado las siguientes modificaciones para integrar la API de elToque con la URL base y estructura de respuesta correctas:
- **`ExchangeRatesViewModel.kt`:**
  - La `BASE_URL` se ha corregido a `https://tasas.eltoque.com`.
  - Los `StateFlow` `_exchangeRates` y `exchangeRates` se han actualizado para manejar `List<ExchangeRate>` en lugar de `ExchangeRateResponse`.
  - La llamada a `response.body()` se ha ajustado para esperar `List<ExchangeRate>`.
- **`ExchangeRateData.kt`:**
  - Se ha actualizado la clase de datos `ExchangeRate` para reflejar la nueva estructura JSON (con `currency`, `buy_median`, `sell_median`, `date`).
- **`MercadoScreen.kt`:**
  - Se ha ajustado la lógica para acceder y mostrar los datos de las tasas de cambio, utilizando la `List<ExchangeRate>`.
  - La fecha de última actualización se obtiene del primer elemento de la lista.
  - Se utilizan `buy_median` y `sell_median` para mostrar los valores.
  - Se ha eliminado el bloque `else` que mostraba un mensaje genérico cuando no había tasas disponibles, dejando el área en blanco en ese caso.