Cambios aplicados el sábado, 4 de octubre de 2025 a las 12:30

Se han implementado las siguientes mejoras en la pantalla de Mercado (`MercadoScreen.kt`):
- **Indicador de tendencia (UP/DOWN) en las tarjetas:**
  - Se ha modificado `DataStoreManager.kt` para almacenar la respuesta anterior de las tasas de cambio, lo que permite calcular la tendencia.
  - Se ha modificado `ExchangeRatesViewModel.kt` para:
    - Definir un `enum class Trend` (UP, DOWN, SAME, NONE).
    - Cargar las tasas de cambio anteriores desde `DataStoreManager`.
    - Calcular la tendencia de cada divisa comparando el valor actual con el anterior.
    - Guardar las tasas actuales como tasas anteriores después de una carga exitosa.
    - Exponer las tendencias a través de un `StateFlow` (`currencyTrends`).
  - Se ha modificado `MercadoScreen.kt` para:
    - Importar los iconos `ArrowUpward`, `ArrowDownward`, `Remove` y el `enum Trend`.
    - Recuperar las tendencias de `currencyTrends` del `ExchangeRatesViewModel`.
    - Mostrar un `Icon` (flecha arriba/abajo/guion) en la esquina superior derecha de cada `Card` de divisa, con colores verde (UP), rojo (DOWN) y gris (SAME/NONE).